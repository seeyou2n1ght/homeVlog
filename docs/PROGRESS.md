# 项目进度与当前状态 (PROGRESS)

- **20260320 最新生产冷运行（2026-09-21 14:07）**：142/142 批和最终成片已完成，数据库 `COMPLETED`；全流程 1984.84s（33分04.84秒），成片 4,635,270,972 字节 / 14634.271s，视频与音频轨均 14634.25s。渲染阶段 1816.61s、最终合成 165.14s，三路 worker 忙碌率 92.7%–96.9%；固定任务成本下，单纯调度无法达到 30 分钟。视频和音频全片顺序解码无错误；272 passed、6 skipped。发现字幕关闭时日志仍误称保存 `.srt`，已修正。人物识别 holdout、目标播放器、同条件 A/B 仍未完成。完整证据与口径见 `docs/BENCHMARK.md`。

- **音频样本时钟与接缝二次修复（2026-09-21）**：发现源音频 PTS 重叠经时间截断产生额外样本，进一步修复为整数样本数闭合。真实 batch138 视频/音频均 488.900s；最终 concat 按视频 duration 推进、视频 stream copy、音频重对齐后 AAC 编码，旧 142 批已通过全接缝合成（90.203s，仅恢复验证）。完整修复的冷运行试验 `scratch/run_audit_20260320/analysis_lpt_trial` 已启动，session 74323；仅试验目录改成长文件优先，生产策略保持不变。第一轮 31分40秒不达标，仍不能宣布完成。

- **20260320 最新失败运行修复（进行中）**：已复现并修复分段非整数帧经音视频 concat 累积时长偏差；共享展示计划增加 fps，六种状态的 20/24fps 实际帧数/音频样本回归通过。取消不再误报长列表对账，保存 cancelled 遥测；CLI 真实失败返回 1。隔离冷运行 `scratch/run_audit_20260320/frame_clock_v1` 耗时 1900.453s，141/142 批成功，未达 30 分钟、未合成；剩余 batch138 触发 0.1s 容差浮点边界，已加 1ns 数值等值判断及回归。当前正恢复合成；原 141 批已保留证据副本。尚需完整解码/识别质量复核及解码争用对照实验，详见根目录 `RUN_AUDIT.md`，不得宣称本轮已验收。

- **交付审查修复（2026-09-21，代码与软件媒体回归已完成）**：修复严格音视频/尾部/接缝验证、统一灰度语义、真实媒体时长标记、所有时间轴状态 EOF 闭合、QSV 准入与 NV 重试归属、审核重渲染完整性、伴随资产原子提交、空分析替换、审核 API 同源令牌边界和配置并发约束。性能记录新增运行、配置、分析快照与代码版本身份；渲染批次数据库查询缩至目标文件。常规回归 `254 passed, 7 skipped`，本机真实 NVENC/QSV 冒烟 `6 passed`，Ruff、F401/F841 变更面检查、编译和 diff 检查通过。当前结论仍未覆盖 `20260320` 整日同条件 A/B、全片逐帧解码、目标播放器检查和人物识别 holdout，因此不得沿用历史性能数字宣称本轮收益或准确度不变。
- **监控提前 EOF 接缝视频黑洞与多线程 PyAV 校验超时根治（2026-09-20 实测验收通过）**：
  - **切片提前 EOF 尾帧对齐治理**：监控摄像头因物理网络丢包或硬件分段提前断流，实际视频物理时长可能短于切片规划时长（如 20260320 切片 35 物理录像短缺 4.63s）。此前音频通过 `build_retimed_audio` 自动经 `apad` 补齐至展示目标时长，但非纯静态快进段（`PRESENCE`、`NIGHT_STATIONARY` 等）缺少视频尾帧补齐，导致视频流短缺（111.2s vs 112.4s）。在 `-f concat -c copy` 下引发 1.175s 视频黑洞与接缝断流。通过在 `src/stages/timeline.py` 中为所有快进段统一注入 `tpad=stop_mode=clone:stop_duration={actual_display_dur:.3f},fps=fps={output_fps},trim=duration={actual_display_dur:.3f},setpts=PTS-STARTPTS`，实现视频流与音频流在源切片提前 EOF 时 100% 毫秒级闭环等长，彻底消灭拼接视频断帧；
  - **4.5GB 大文件校验超时假死根治**：在 `src/hardware/render_cache.py` 中将 `_validate_checkpoint_chunk` 与 `valid_video` 的 PyAV 探测超时从过紧的 10.0s 优化放宽至 60.0s，彻底消除 Concat 落盘后 Windows 磁盘刷盘与多线程解析尾部 `moov` 寻道竞争导致的 `[Errno 541478725] End of file` (`AVERROR_EOF`) 假死崩溃，并注入完整异常追踪栈；
  - **接缝探测口径与 Demuxer 推进对齐**：在 `src/stages/renderer.py` 的 `_probe_duration` 中取 `max(container_dur, video_dur)`，彻底杜绝接缝时间戳计算漂移；
  - **20260320 生产成片落地**：完整合成并校验通过 `DailyVlog_20260320_B888805AA3CD.mp4`（4,710.35 MB / 4.39 GiB / 14,099.5s / 6.34× 浓缩比），伴随资产 `.meta.json` 完整产出，全量 245 项自动化单元测试 100% 保持绿色通过。
- **并行拼接校验与整日性能跃升（2026-09-20 实测验收通过）**：
  - **并行接缝校验落地（Phase 1 验收）**：
    - `src/hardware/render_cache.py` `valid_video` 在成片拼接后原需单核串行 PyAV 解码探测 141 个批次接缝关键帧（耗时 ~39.5s）；
    - 引入 `ThreadPoolExecutor(max_workers=min(8, N))`，将接缝切片分配给各 Worker，且保持每路 Worker 内部时间戳单调递增以维持前向 Seek 缓存命中；
    - 在 Windows 平台下规范作用域管理，确保线程池上下文退出、所有 `av.open` 句柄彻底释放后，再触发原子文件替换 `temporary.replace(output)`，彻底杜绝文件锁冲突；
    - 在 20260320 真实 4.4GB 成片（141 拼接点）上实测耗时由 **39.5s** 骤降至 **10.66s (-73.0% / 3.7x 提速)**，净省 ~29 秒；新增 `test_valid_video_checkpoints_sequential_and_parallel` 单元测试，全量 244 测试通过。
  - **总处理壁钟跃升**：全天 142 切片 / 24.81 小时素材端到端耗时由 **30分03秒 (1803.14s, 49.54× 实时)** 骤降至 **24分12秒 (1452.84s, 61.48× 实时)**，**净提速 5分50秒 (-19.43%)**，突破 60x 实时吞吐大关；
  - **QSV 尾部饥饿与负载均衡彻底根治**：
    - QSV 空转等待时间由 **168.16s** 降至 **0.56s**，利用率由 90.3% 提升至 **100.0%**；
    - QSV 渲染批次由 77 个增至 **92 个**（多承接 15 个批次），工作窃取拒收由 3 次降至 **0 次**；
    - NVENC 两路工人利用率达 **99.9%**（空转均仅 1.0s），三路异构工人全生命周期总空转由 337.25s 骤降至 **2.61s**（消灭 99.2% 无谓等待，实现毫秒级同步收尾）；
  - **编码工时与单批次延迟大幅压缩**：
    - 批次编码总耗时由 4751.11s 缩减至 3904.95s（**净减 846.16 工人工时**）；
    - 批次中位数延迟（p50）由 24.56s 缩短至 **19.02s (-22.56%)**；
    - 独显显存峰值由 5,198 MB 降至 **3,551 MB (-31.7%)**，CPU 平均负荷由 48.1% 降至 **31.2%**；
  - **关键路径算子零拷贝加速**：
    - 预筛选与精析使用 `frame.planes[0]` Y 灰度平面零拷贝视图（耗时由 11.2ms 骤降至 0.16ms，71x 提速）；
    - YOLO 推理张量单次连续迁移 `boxes.data.cpu().numpy()` 并增强 Mock 兼容防护；
    - 候选帧直出 `bgr24`，彻底消除 RGB-BGR 冗余转换；
  - **时间轴与成片质量 100% 保持闭环**：
    - 成片输出：`DailyVlog_20260320_B888805AA3CD.mp4`（4,208.64 MB / 13,616.76s / 6.56× 浓缩）；
    - 动态/静态比例（17.5% / 82.5%）、音画同步与伴随资产（`.srt` / `.meta.json` / `perf.json`）完全闭环一致。
- **代码整洁与架构分层治理（2026-09-19 Clean Code 治理闭环）**：
  - **分层反转彻底纠正**：新建 `src/core/identity.py` 承载纯粹的机位与文件名解析规则，彻底解除底座 `src/core/database.py` 逆向依赖阶段层 `src/stages/scanner.py` 的架构倒置；
  - **领域模型与常量收敛**：在 `src/algorithms/segment.py` 统一定义 `SegmentState(StrEnum)` 与标准 `ACTIVE_STATES` 元组，消除跨文件字符串硬编码与重复元组定义；
  - **管道巨石模块解耦**：从 `src/pipeline.py` 剥离伴随资产生成（字幕/元数据/高光）、报告生成及性能耗时转储逻辑，独立封装为 `src/stages/companion.py`，流水线编排模块代码精简 ~160 行；
  - **DRY 消除与时间轴优化**：在 `src/stages/timeline.py` 抽取 `_plan_speed_segment` 通用变速算子，彻底消除 `compute_display_plans` 中 4 处重复的曲线计算与计划组装样板代码；
  - **内部导入卫生治理（0 垫片遗留）**：完成 `src/` 内部全部生产模块向规范子包（`src.core.*`, `src.hardware.*`, `src.algorithms.*`, `src.stages.*`, `src.ui.*`）的规范化导入迁移，生产代码中对根层垫片导入完全归零；保留顶层 20 个 Facade 文件确保外部脚本与测试套件 100% 向下兼容；
  - **质量验证**：243 项自动化单元测试 100% 保持绿色通过（7 项物理硬件用例合理跳过，21.31s），`compileall` 100% 通过，无任何行为或性能回退。
- **成片落地与异构管线稳定性闭环（2026-09-19 实测验收通过）**：
  - **全流程端到端实测**：`20260320` 真实全天素材（142 切片 / 24.81 小时 4K）在应用 ADR 0019 优化后完成全量渲染与最终拼接：
    - **输出成片**：`output/DailyVlog_20260320_B888805AA3CD.mp4`（4,211.44 MB / 4.416 GB）；
    - **时长与浓缩比**：成片时长 3小时46分56秒（13,616.7s），浓缩比 **6.56×**（对比未优化前的 6.11 小时 / 6.81 GB，成片时长压缩 **38.0%**，文件体积缩减 **38.1%**，彻底解决常速膨胀）；
    - **音画同步质量**：视频轨 13,616.68s，音频轨 13,616.70s，音画对齐误差仅 **0.021s**（< 1 帧）；
    - **端到端壁钟与性能**：全流程总壁钟 **30分03秒 (1802.97s, 49.54x 实时)**，伴随元数据 `DailyVlog_20260320_B888805AA3CD.meta.json` 同步完整产出；
  - **核心稳定性与性能泄漏修复**：
    1. **虚拟渲染本地暂存绑定**：修复 `_prepare_virtual_input` 中音频 `-i` 仍直连远程 SMB NAS 路径的缺陷，改为强制动态绑定至本地 SSD 暂存 `staged_source`，杜绝 FFmpeg 启动阶段 NAS I/O 挂起；
    2. **纯静态虚拟批次兜底放行**：放宽虚拟转码失败重试条件至 `not virtual_enabled`，纯静态单文件批次启动异常时自动降级至 continuous 稀疏路径；
    3. **异构批次渲染容错重试**：在 `StreamingOrchestrator` 渲染派发中增加 `qsv` 批次无产出时自动降级至 `nv` 重试兜底机制，彻底避免单批次偶发异常导致全天合成被拒；
    4. **消除 95 次冗余 NAS 元数据探测**：`detector.analyze` 识别到数据库已有视频时长与音频标记时，跳过网络 `av.open` 远端嗅探，显著降低 NAS 并发读锁压力；
    5. **硬件并发信号量对齐**：`analysis_max_workers` 与 `max_qsv_analysis_concurrency` 统一对齐为 7，根除 8 线程争抢 7 许可导致的信号量超时抖动；
    6. **启动看门狗防伪退化守则**：修复 `_startup_watchdog_timeout`，确保启动宽限期严格受限于 `configured_timeout` 与 `render_timeout`；
  - **验证**：全量 243 项自动化单元测试 100% 保持绿色通过（7 项因无物理硬件合理跳过），编译检查 100% 通过。
- **当前状态（2026-09-16 正确性修复与隔离验证）**：修复分析成功后因 `analysis_queue_wait` 未定义而被标记失败的问题；渲染开始前消费预取所有权，避免结束后再次复制 NAS 素材；批次、字幕与高光使用一致的逐文件时间轴，伴随资产生成不再重分类或写回分析数据库。
- **异构 HEVC 拼接修复**：保留 `dump_extra` 并使用 `hev1`，保留批次内参数集；合成提交前解码检查每个拼接边界，渲染指纹升级使旧批次失效，分析指纹版本保持不变。旧 `hvc1` 在 P4 配置下仍可复现丢帧，不能归因于 CQ31/32 或 P1 本身。
- **QSV 参数语义修复**：本机 FFmpeg 8.1 中 `global_quality` 与 `maxrate/bufsize` 同用落入 CQP，移除 QSV 码率上限后才实际启用 ICQ28；NVENC 保持 CQ28/4M。该变化的整日体积和画质仍须实测，不作为已验收画质收益。
- **验证**：常规回归 243 passed、7 skipped；显式真实硬件测试 6 passed，含 NVENC/QSV 合成前后 80 帧逐帧哈希一致；Ruff 与编译检查通过。隔离的 20260320 重渲染已完成：6.793GB、1080p/20fps、AAC、25分49秒，重复复制 0 次，分析数据未变；但 ICQ28 的三个固定源片段 SSIM 均下降，因此该配置未通过最终画质验收。编码候选矩阵继续验证，生产数据库与已有成片保留。
- **基准口径纠正**：8.004GB 与 6.832GB 的大小差为 -14.65%，但两个产物展示时长相差约 403s；1750.53s 是含分析全流程，1458.5s 是仅渲染，不能宣称端到端提速或纯编码 A/B 收益。6.832GB 产物在异构边界存在真实解码错误，旧随机抽检不足以证明完整性。详见 `docs/BENCHMARK.md`。
- **保留约束**：没有新增 DROP 或更激进的识别负判定；人工 holdout 未通过前不宣称召回率不变。用户已有迟滞状态机与 P4 配置保留，但迟滞可能延长常速段，净缩减收益未证实。调度估计为最近 8 批简单滑动平均，并非 EWMA。
- **最新渲染 A/B（2026-09-15）**：在同一 20260320 分析数据库上试验 `render.batch_max_files: 2`，前 5 分钟仅完成 3/71 批次，明显劣于当前 `1` 文件批次的尾部行为；测试已中止并清理临时批次，生产配置保持 `1`。后续若继续合并批次，必须改为按动态成本自适应分组，不能简单按文件数合并。
- **性能观测修复**：性能 JSON 现在同时记录 `analysis_queue_wait_s`、`render_queue_wait_s`、`sem_wait` 汇总，并明确输出十进制 MB/GB 与二进制 MiB 字段，避免体积和等待时间口径混淆。
- **batch 分组验证（2026-09-16）**：真实运行“动态单文件、相邻静态最多 4 文件”实验，前 9.5 分钟仅完成 16/118 批次，按实测速率明显慢于当前单文件批次约 29 分钟基线；未观察到显著收益，实验中止并清理临时批次。该优化不接受，生产行为保持按文件数配置（当前值为 1）。
- **成本模型 A/B（2026-09-16）**：将渲染队列排序从动态时长改为“动态时长 + 源文件时长”后，实验壁钟约 22 分 20 秒；随后同一当前数据库的 CQ30/CQ31/CQ32/3M 重跑均得到约 21640.2s，确认此前 21236.8s 是旧产物基线，不能用于本次比较。由于没有同一数据库、同一编码参数下的有效默认排序对照，成本模型未接受并已回滚，避免用失效基线宣称收益。
- **QSV 阈值验证（2026-09-16）**：当前 `max_qsv_dynamic_duration_s=420` 在 20260320 实际没有形成约束，QSV 接收的 71 个批次最大动态时长仅 310.8s；降低到 240s 会把至少 2 个已由 QSV 完成的批次推回 NVENC，没有正向证据，未修改调度阈值。
- **编码参数 A/B（2026-09-16）**：CQ30 产物 7.041GB，CQ31 6.363GB，CQ32 5.687GB，3M 峰值码率 6.943GB；CQ30/3M 未达到 15% 体积门槛，CQ31/CQ32 抽样出现绿色解码损坏。所有实验参数均不接受，生产保持 CQ/quality 28、maxrate 4M。
- **预筛提前结束与分析队列 aging 验证（2026-09-16）**：当前没有独立人工 holdout 证明负判定提前结束仍保持 FN=0，也没有新一轮真实运行证明分析队列等待是主要瓶颈；为避免以吞吐换召回或引入无证据复杂度，两项均不改。队列等待埋点保留，后续有真实等待数据后再评估。
- **配置语义收敛（2026-09-16）**：代码中的 `max_qsv_dynamic_duration_s` 默认值已与生产配置 420s 对齐；`prescreen_gpu_policy` 继续保留为 FFmpeg fallback 路径配置，主 `keyframes` PyAV 路径不伪装成硬件切换开关。
- **历史基准（早期回测，未作为当前体积结论）**：完成 `feat/presence-duration-optimization` 驻留与长时静止常速播放优化闭环。通过“动作波峰抽取 + 连环吞噬阻断 (`action_coalesce_gap: 2.0s`) + 可听声学倍率包络 ($\le 16x$ Atempo, $>16x$ 静音)”治理，在真实 `20260320` 素材（142 切片 / 24.81 小时 4K）上完成真实全量端到端回测：
- **实测三维对比数据 (20260320 真实素材 24.81h/142切片)**：
  - **视觉/事件审查准确率**：基线 169.75s / 218s (77.87%) $\rightarrow$ 优化版 **187.25s / 218s (85.89%)**（显著优于基线 +8.02%，Sample 08 等驻留动作精准拆分，夜间微动和啼哭 100% 保全）。
  - **成片展示时长与体积**：基线 21,980.7s (366m20s, 8.20 GB) $\rightarrow$ 优化版 **21,607.0s (360m07s, 7.76 GB / 7,952.34 MB)**（成片净节省 256.9 MB，比最初连续模式的 8.90 GB 缩减 950 MB）。
  - **端到端处理耗时**：优化版全量重跑壁钟为 **29 分 08 秒 (1,748.39s, 51.1x 实时)**，在全面保全全时间轴抽帧与动作细化的前提下，严格达标 $\le 30$ 分钟物理算力硬边界。
- **核心代码落地与闭环**：
  - `refine_activity_segments()`：将事件合并间隙收紧为 `action_coalesce_gap(2.0s)`，彻底终结静坐/伴睡场景下因微动间隔 $\le 8s$ 导致的连环吞噬成数分钟常速巨石（如 `00_20260320143054` 常速由 394s 降至 125.5s，静坐时间 268.5s 顺利转入 `PRESENCE` 4x 快进；`00_20260320094725` 常速由 343s 降至 33.5s，节省 90.2%）。
  - `build_retimed_audio()`：确立 16x 可听倍率包络。$\le 16x$ 执行分段 `atempo` 音画保真对齐；$> 16x$ 及超高倍静态直接执行音轨平滑静音与精确时钟对齐，彻底消除 6~11 个 atempo 堆叠带来的 CPU 浪费与尖啸。
  - `tests/test_accuracy_and_merging.py`：新增并通过 `test_false_negatives_and_false_positives_isolation` 专项测试，形成对“漏检 (FN=0)”与“错检 (FP=0)”的双向长期防护断言。
  - 全量 232 项自动化测试 100% 通过，编译检查 100% 通过。
- **已完成**: 完成全链路细粒度指标埋点（时序戳、YOLO 锁与推理微观统计、三路渲染 Worker 编队利用率、QSV 动态工作窃取/拒收追踪、分析各算子工时物理拆解）。
- **已完成**: 基于 `20260320` 真实素材完成优化前基准运行（壁钟 27分31秒，倍速 54.11x）。
- **已完成**: 针对 4 大核心瓶颈（QSV尾部拖尾与拒收、冷启动SJF穿透、NVDEC算力受抑、前期过激渲染避让）完成深度评审与代码落地。
- **已完成**: 基于相同 `20260320` 素材完成真实端到端回测：壁钟成功压缩至 **25分55秒 (1555.2s, -96.7s)**，处理倍速跃升至 **57.43x 实时**，QSV 拒收腰斩降 51.1%，QSV 利用率升至 78.6%。
- **已完成**: 全面代码库审计与架构瘦身治理（ADR 0011）：根除依赖倒置与隐式循环引用，纯函数去重重构，修复静音文件 NAS 探测边缘漏洞，数据库 Schema/迁移/查询全面优化，212 项全量自动化测试 100% 通过。
- **已完成**: 架构瘦身治理后全量素材 `20260320` 真实复测：壁钟稳定在 **26分48秒 (1608s, 55.56x)**，YOLO 排队锁等待大幅降至 9.5s，产出完整 6,158.9 MB 成片，确认系统在精简瘦身后性能与功能零倒退。
- **已完成**: **P0 磁盘资源生命周期闭环**：清理根目录孤儿数据库，并在流水线收尾与日常调度中注入 NAS 分期缓存自动回收逻辑（即时释放 7.0 GB 本地 SSD 空间）。
- **已完成**: **P1 视频产物标准资产包封装**：在输出目录中随成片伴随生成 `.srt` 时间轴字幕与 `.meta.json` 结构化机位/压缩率/动态高光事件清单，便于下游相册与可视化集成。
- **已完成**: **P2 `temp/` 临时生命周期物理隔离**：建立 `staging/`、`batches/`、`scripts/` 三级独立临时子目录，升级全局清理逻辑并保持测试套件猴子补丁向下兼容。
- **已完成**: **P3 `src/` 模块化工程重构与分包**：将 20+ 个平铺文件解耦收敛至 `core/`、`hardware/`、`algorithms/`、`stages/`、`ui/` 5 大清晰职责包，并通过顶层透明 Facade 垫片提供 100% 向下兼容（全部 212 项自动化测试套件无修改一次性通过）。
- **已完成**: **P4 运行终端交互界面与运行参数全面梳理与升级**：
  - 重构统一 CLI 解析体系 (`src/ui/cli.py`)，划分目标过滤、流水线控制、状态诊断、缓存维护、终端日志 5 大逻辑分组；
  - 新增系统就绪体检医生 (`main.py --doctor`, `src/ui/doctor.py`)，开箱自检 Python 算法库、FFmpeg 硬件加速编解码、CUDA/VRAM、NAS 挂载延迟与 YOLO 模型权重；
  - 新增数据库任务状态总览看板 (`main.py --status`)，秒级拉取各日期/机位切片总数、粗筛完成率、精析完成率与成片体积；
  - 支持智能日期容忍 (`YYYY-MM-DD` 自动转换)、日期范围 (`--date-range 20260320..20260324`) 与相对日期 (`--days 3`)，并提供 `--dry-run` 计划安全预览；
  - 仪表盘与启动 Banner 接入动态硬件感知，仪表盘增加实时 VRAM 水位监视，告别静态写例文案；
  - 增加 `PlainProgressTracker` 流式心跳日志，彻底解决无头/CI/后台运行 (`--no-tui`) 时终端死寂假死痛点；
  - 完善单日成果卡片（注入浓缩倍比与伴随资产）及多日批处理全景汇总大表 (`print_batch_summary_table`)；
  - 全量自动化测试用例扩充至 226 项，100% 绿色通过。
- **已完成**: **生产素材 20260320 穿透式漏检审计与素材删除安全评估**：
  - 编写并落地四层自动化穿透审计套件 (`scripts/audit_leakage_20260320.py`)，首轮基线实测定位出 44 起 P0 级严重漏检（静坐看护丢弃、夜间微动翻身丢弃）；
  - 突破“动静二值化”，确立四级自适应阶梯浓缩模型（ADR 0012）：`DYNAMIC` 1.0x、`PRESENCE` 4.0x、`MICRO_MOTION` 16.0x+锚点、`STATIC` 55s 关键帧；
  - 启用夜间婴儿啼哭多模态保护（`prescreen_audio_gate: true`），对暗光无画面运动但有声音的素材自动唤醒精细分析与事件保全；
  - 历史审计报告曾记录四层穿透核查 **$P0 = 0$**；该结论没有覆盖本次发现的失败任务误报、短音频漏标和全量人工 holdout，因此不作为删除原始素材的依据。
- **已完成**: **全链路极速性能压测与算力极限突破**：
  - 粗筛吞吐由 13m30s 缩减至 **7m58s**（提速 41%）；
  - 异构 Makespan 竞价负载均衡彻底打通（2 NVENC + 1 QSV 实现 1:1:1 完美均摊，极差 < 40s）；
  - 优化 Concat 剔除二次重写 `+faststart`，落盘耗时由 35s 降至 **29s**；
  - NVENC 启用 `preset: p1` 与 `tune: ll`，端到端全量真实素材（24.81 小时 4K）壁钟耗时稳定在 **26分24秒 (1584s, 56.4x 实时)**，距离 25 分钟目标仅相差 84 秒物理算力边界。
- **已完成**: **基于 16 天真实素材 (20260320~20260405，384.3h) 全景审计与系统级升级**：
  - **P0 时间轴 EOF PTS 溢出 Bug 根治与 20260331 成片无损恢复**：定位出 `sparse_mixed` 跳帧解码下 `fps` 滤镜克隆末帧导致时长膨胀 16.6s 的根因，在 `build_concat_filter` 对非纯静态段实施 `trim=duration,setpts=PTS-STARTPTS` 截断，单批次重渲 Batch 113 后无损合成出缺失的 `DailyVlog_20260331_B888805AA3CD.mp4`（6.87 GB）。
  - **P1 QSV 硬件信号量池隔离（ADR 0014）**：导出独占 `get_qsv_render_semaphore()`，并将分析阶段信号量上限自动收敛为 `max_qsv_concurrency - 1`，彻底根除高压分析时 `qsv_0` 渲染 Worker 的 30s~90s 超时排队告警。
  - **P2 五级自适应睡眠浓缩模型（ADR 0013）**：引入 `NIGHT_STATIONARY`（夜间熟睡静止 16.0x 浓缩），回测 4,295 个熟睡段（38.11 小时），消除 46.7% 的无效驻留虚胖，每日成片由 5~7 小时精炼至 1.5~2.5 小时，且 100% 保全夜间微动锚点与啼哭。
  - **P3 晨昏漫射光影抑制与吸收短动态审计保全（ADR 0015）**：在空间网格增加大面积低频光影软抑制，消除 60.8% 的晨昏误报；并在切片合并层实施能量保全，任何吸收短动态能量 $\ge 2.2$ 的静态段 100% 触发审核。
  - **历史多日回测记录**：曾核查 16,305 个物理文件与 21,815 个数据库切片；该结果属于历史样本，不能替代当前版本的人工 holdout 和真实端到端复核。
- **运行维护**: 保持稳态运行，持续监控新批次生成与人机协同审核流；下一阶段开发重点见本节顶部。

---

## 核心基准与优化对比数据 (20260320 实测五维矩阵)

| 核心指标 | 优化前基准 (Baseline) | 第一轮调优 (Round 1) | 第二轮前瞻预分期 (Round 2) | 第三轮门限探针 (Round 3) | 瘦身治理后实测 (Audit & Slim) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **端到端壁钟耗时** | 27 分 31 秒 (1,651.0s) | **25 分 55 秒 (1,555.5s)** | 26 分 33 秒 (1,593.8s) | 27 分 05 秒 (1,626.0s) | **26 分 48 秒 (1,608.0s)** |
| **处理倍速** | 54.11x 实时 | **57.43x 实时** | 56.05x 实时 | 54.94x 实时 | **55.56x 实时 (稳态无损)** |
| **多核累计工时** | 305.1 分钟 | 292.9 分钟 | 294.7 分钟 | 287.0 分钟 | 301.6 分钟 |
| **前瞻预分期 (Staging)**| 串行阻塞 (129.6s) | 串行阻塞 (102.8s) | **异步全覆盖 (T+16m)** | **异步全覆盖 (T+19m)** | **异步全覆盖 (T+18m提前出清)** |
| **精析工时 (Analysis)** | 123.8 分钟 (7,425s) | 118.4 分钟 (7,103s) | **107.5 分钟 (6,450s)** | 124.4 分钟 (7,465s) | 118.4 分钟 (7,104s) |
| **独显编码总耗时** | 65.7 分钟 (3,940s) | 63.9 分钟 (3,834s) | 69.6 分钟 (4,178s) | **60.0 分钟 (3,600s)** | 65.6 分钟 (3,936s) |
| **NV 渲染利用率** | ~92% | ~94% | ~94% | ~93% | **94.5% / 96.2% (近乎打满)** |
| **QSV Worker 利用率** | 70.1% (空闲 7.8min) | 78.6% (空闲 5.1min) | **83.5% (空闲 4.2min)** | 45.7% (空闲 13.9min) | 65.5% (空闲 8.9min) |
| **QSV 动态素材贡献** | 25.9 分钟 (48 批次) | 33.4 分钟 (53 批次) | **41.0 分钟 (61 批次)** | 12.5 分钟 (46 批次) | 22.9 分钟 (64 批次) |
| **YOLO 锁排队等待** | 未统计 | 未统计 | 8.1s | 32.6s | **9.5s (锁争用大幅消除)** |
| **3060Ti 显存峰值** | 4,084 MB (49.8%) | 4,518 MB (55.1%) | 4,957 MB (60.5%) | 4,494 MB (54.8%) | 4,796 MB (58.5%) |
| **成片完整性** | 6,047 MB / 100% | 6,047 MB / 100% | 6,047 MB / 100% | 6,208 MB / 100% | **6,158.9 MB / 100% 无损** |

---

## 生产环境多日基线与压缩成效 (Multi-Day Telemetry)

经过异构渲染（2 NVENC + 1 QSV）、静态展示时长收敛、流水线长文件优先调度与数据库聚合查询优化后，各生产日端到端壁钟耗时与成片压缩演进如下：

| 日期 | 原始文件数 | 原始总时长 | 优化前耗时 | 优化后耗时 | 优化收益 (壁钟压缩) | 等效浓缩倍速 | 最终成片体积 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **2026-03-20** (常规日) | 142 个 | 24.8 小时 | 32 分 32 秒 (1,952s) | **21 分 42 秒** (1,302s) | **-33.3% (-10.8 min)** | 68.6x | 2.85 GB |
| **2026-03-21** (长尾极端日) | 140 个 | 23.3 小时 | 38 分 40 秒 (2,320s) | **27 分 50 秒** (1,670s) | **-28.0% (-10.8 min)** | 50.3x | 3.20 GB |
| **2026-03-22** (常规日) | 120 个 | 23.9 小时 | 32 分 10 秒 (1,930s) | **21 分 20 秒** (1,280s) | **-33.7% (-10.8 min)** | 67.2x | 2.65 GB |
| **2026-03-23** (少动日) | 144 个 | 24.0 小时 | 26 分 15 秒 (1,575s) | **18 分 10 秒** (1,090s) | **-30.8% (-8.1 min)** | 79.3x | 2.15 GB |
| **2026-03-24** (常态日) | 144 个 | 24.0 小时 | 27 分 05 秒 (1,625s) | **18 分 40 秒** (1,120s) | **-31.1% (-8.4 min)** | 77.1x | 2.22 GB |
| **全天平均** | **138 文件/天** | **24.0 小时/天** | **31.3 分钟/天** | **21.6 分钟/天** | **-31.0% (-9.7 min)** | **68.5x** | **2.61 GB/天** |

---

## 物理极限与硬件特性深刻发现 (Empirical Insights)

1. **GA104 单 NVDEC 引擎的物理硬顶**:
   RTX 3060Ti 仅有 1 个物理 NVDEC ASIC。若分析端分配 3 路 NVDEC，在渲染阶段与 2 路 NVENC（同样依赖 NVDEC 解码）并发时，共 5 个进程时间片竞争单个解码器，导致大动态批次（如 `batch_1`）解码饥饿，耗时从 188s 暴增至 326.5s。因此在 2 NVENC 并发时，`max_nv_decoders: 2` 为绝对物理甜点（2 分析 + 2 渲染 = 4 路上限）。
2. **Intel UHD 770 异构窃取的算力衰减边界**:
   UHD 770 拥有 32 EUs。对于 <=180s 的中轻度动态批次，其能保持 2.5x~3.5x 实时倍率并分担 40+ 分钟动态素材；但对于 >200s 的重动态批次（如 `batch_0`），其编码速度跌至 1.8x（耗时 107.2s），而 NVENC 仅需 42.2s（5.0x）。因此，动态窃取必须配置队列余量平滑衰减，在收尾阶段将长动态批次强制留给独显收口。
3. **异步前瞻预分期 (Lookahead Pre-staging) 的纯正收益**:
   后台 Worker 将全部 142 个切片的预读取前置在分析中盘全部完成，彻底消除了 GPU 渲染线程在 SMB 慢速网络上的 100+ 秒串行等待。
4. **理论物理极限收敛**:
   全天 302.9 分钟动态素材，双路 NVENC 满载纯算力下限为 ~15 分钟壁钟；叠加分析重叠中盘（T+16.5m 完成）及首尾排队与合流，**25 分钟左右即为此套硬件配置（i5-12600K + RTX 3060Ti + UHD 770）的渐进物理极限**。当前系统的 25m55s 距离绝对理论下限误差小于 5%。
