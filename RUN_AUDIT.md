# 20260320 真实运行审计与修复验收

状态：2026-09-21 14:07 的最新生产冷运行已完成成片；全流程 1984.84s，仍未达 30 分钟。视频和音频全片顺序解码无错误；识别质量和目标播放器仍待验收。以下初始失败、隔离试验和旧执行句柄均为历史记录，不代表当前进程状态。

当前证据：`logs/runs/homevlog_20260921_133428.log`、`logs/perf/perf_20260320_cam0_20260921_140736.json`、`data/vlog.db`、`output/DailyVlog_20260320_B888805AA3CD.mp4`。142/142 批完成，最终任务 `COMPLETED`，无存活的生产 Python/FFmpeg 任务；详情见 `docs/BENCHMARK.md`。

## 当前运行身份及第一手证据

- 日志：`logs/runs/homevlog_20260921_001951.log`；对应 `logs/error/error_20260921_001951.log` 和 `logs/events/events_20260921_001951.jsonl`。
- 流水线从 00:19:56 开始，00:54:53 用户 Ctrl+C，日志记录壁钟 2097.7 秒。扫描开始时间为 00:19:51；两种计时边界不可混用。
- 当前数据库 `data/vlog.db` 只读查询：20260320 共 142 文件，43 STATIC、99 SUSPICIOUS/ANALYZED；当日 `render_tasks` 无记录。STATIC 对应的 analysis_status=PENDING 不等于分析漏跑，需按预筛路由解释。
- 当前 `output/` 无 MP4。因此历史文档中的成片和性能结果不能充当此次运行的成片验收证据。
- 核查时无存活 Python/FFmpeg 进程；此次运行已终止，不应当作后台任务继续等待。

## 已确认问题及待验证根因

1. 日志有 155 次渲染结束记录，96 次 output=True、59 次 output=False（包含取消）。无产出的累计 worker 时间 2830.254 秒，这是并发累计工时，不是可直接节省的壁钟时间。
2. 63 次时长拒绝，差值 0.106–0.481 秒；包含批次内部 fallback，所以不等于 63 个独立失败批次。多次 QSV→NV 重试得到完全相同的时长偏差，例如 batch133 两次均 202.650 秒，而计划为 202.268 秒。这是确定性时间轴问题的强线索，而非换编码器即可解决的瞬时故障。
3. `compute_display_plans()` 返回连续秒数；分段滤镜各自 `fps` 后按 `trim=duration` 截断，再由音视频 concat 拼接。已用当前 `build_concat_filter()`、20fps 64×64 testsrc2、48kHz sine、libx264/AAC 真实软件媒体复现（非 Mock）：20 段各 1.026 秒，计划 20.520 秒，视频 21.000 秒，音频 20.976 秒；各 1.033 秒时计划 20.660 秒，视频 21.000 秒，音频 20.983 秒；各 1.013 秒则视频 20.250 秒、音频 20.260 秒。分段视频取整后 concat 按较长流推进，正偏差累积。实验产物位于系统临时目录 `homevlog-clock-64olm7tv` 和 `homevlog-clock-ubl8cg68`。不能简单扩大 valid_video 容差来替代修复。
4. Ctrl+C 后仍执行完整性错误对账并打印所有 dispatched/produced ID，缺失列表为 127、131、126、137。取消路径必须与真实静默丢批区分，正常结束时仍须严格拒绝缺失批次。
5. 本次没有 perf JSON。`process_date_streaming` 在 `orchestrator.run()` 抛出 KeyboardInterrupt 时绕过后续 `_dump_perf()`，导致最需要分析的失败/取消运行丢失算子和排队统计。应保存明确标记为取消的遥测，不能将其记录为完整性能基准。

## 识别数据初查

99 个已分析文件包含 1682 段：557 DYNAMIC、227 DYNAMIC_AUDIO、609 PRESENCE、89 NIGHT_STATIONARY、55 MICRO_MOTION、145 STATIC。逐文件相邻段无超过 1ms 的缝隙。这只证明已持久化段的内部邻接关系；不证明首尾覆盖、真实人物召回或预筛 STATIC 文件的负判定准确性。数据库保留了 INSUFFICIENT_YOLO_EVIDENCE 等审核原因，应在后续真实片段复核中优先抽取。

## 验收尚欠

完整代码/算法/调度和测试集评估；复现后修复；针对性回归；同条件整日运行及完整成片解码、音频和时间轴检查；识别质量证据；端到端低于 1800 秒的真实测量。历史绿色测试和 worker utilization 不能代替上述验收。

## 第一轮修复与真实冷运行

已修复整数帧展示计划、取消对账和取消遥测；12 项真实软件媒体回归覆盖六种状态与 20/24fps，验证帧数、逐帧 PTS 连续性和 PCM 样本数。第一轮全套 269 passed、7 skipped；显式硬件冒烟 6 passed。

隔离快照：`scratch/run_audit_20260320/frame_clock_v1`，全新分析数据库；原数据库备份和代码散列见其 `evidence/`。01:08:21 启动，进程耗时 1900.453 秒，流水线记录 1896.11 秒，**未达 30 分钟且未完成合成**。141 批成功；batch138 的 489.000−488.900 秒位于声明的两帧容差边界，却因浮点表示被拒绝。比较增加仅 1ns 的等值容差，并增加 2−1.9 边界接受、1.899 拒绝的真实媒体测试；未扩大帧级容差。

本轮性能 JSON：`logs/perf/perf_20260320_cam0_20260921_014000.json`（路径相对于快照）。预筛结束约 T+363s，分析结束约 T+1480s，渲染结束约 T+1893s。NVDEC 平均 94.6%，NVENC 30.9%，CPU 33%；NV worker 利用率 95.3–95.6%、QSV 96.5%。这是解码与工作分配的瓶颈证据，不支持简单增加编码器并发。分析有 30 CUDA、50 QSV、19 adaptive 记录；长文件分析最高 633.5 秒，渲染最高 344.3 秒。

还发现指定日期 CLI 在失败时打印绿色勾号文案并退出 0，现已改为失败文案与退出码 1，成功/失败两分支测试通过。

恢复验收已启动：保留 141 个成功批次的硬链接证据副本，再在同一快照内仅更新数值边界校验和 CLI，补渲失败批次并尝试合成。恢复时间将单独记录，不能当作冷运行速度。

恢复并非一定只编码一个批次：manifest 指纹含编码器，重新调度到另一编码器的批次仍会重编码。这保证配置身份严格，但可能损失断点恢复速度；不能将 worker 的“已完成数”直接解释为新编码批次数，必须交叉检查 `Reusing verified batch` 日志。

## 下一项受控性能实验

已准备 `scratch/run_audit_20260320/analysis_lpt_trial`，尚不改变生产调度。唯一性能变量为 analysis ready queue 从短文件优先改成长文件优先；冷运行结果显示短文件优先把需要 8–10 分钟分析的夜间长文件推到后半程，存在全局尾部代价。维持编码参数、识别门槛及并发数，验证总耗时和分析数据后才决定是否保留。不得与恢复任务同时跑，避免硬件争用污染结果。

## 尚未验证的架构风险

- 预筛的相邻活动保护读取前一个文件已完成的结果；并行下“尚未完成”当前被当作未活动。该保护存在时序依赖，真实 FN 影响尚未量化；后续比较需区分这些结果差异与识别算法修改，不能凭“没有改阈值”宣布输出完全相同。
- 两次较长的合成音频/视频定位实验分别在 120s/60s 超时，子进程由 subprocess 超时清理。这些未完成实验不算验证通过；已完成的 12 项短媒体测试与真实整日批次证据单独记账。

## 第二层时钟根因与修复证据

恢复运行实际 353.797s，正确返回退出码 1：全部批次成功，但合成在 3456.806、8646.900、12546.821 秒处被连续性门禁拒绝。相应前置批次为 18、96、131，其音轨比视频长 42、42、34ms；concat 默认按容器时长推进，使视频帧间隔超过 1.5 帧。输出未被提交，批次保留。

1. **源音频 PTS 与样本数不等价**：时间截断后 `asetpts=N/SR/TB` 会把重叠 PTS 中额外的音频样本保留下来。以 `sine,...asetpts=0.98*PTS` 模拟摄像机时间戳，旧代码计划 420 帧 / 1,008,000 样本，实际 428 帧 / 1,028,091 样本。修复为每段与每个变速子段按 `end_sample` 截断，变速段内部 concat 后再次闭合样本数。六种状态、20/24fps 的 12 个真实媒体用例全部通过，新增真实时间戳重叠输入，不能再被纯理想时钟样本掩盖。
2. **真实 batch138 复验**：`scratch/run_audit_20260320/sample_clock_probe/result.json`，NVENC 33.906s（仅该批次，不与并发冷运行比较）；视频与音频均 **488.900s**。此修复消除了原来的 0.100s 偏差，不是依赖放宽容差。
3. **最终拼接明确视频时钟**：ffconcat 写入每批视频 duration；保留视频 stream copy，音频解码后按 PTS 消除编码延迟与重叠并重新 AAC 编码。仅 stream copy 音频的真实双批实验触发 non-monotonic DTS，不能作为正确修复。新路径双批 430.200s 视频 / 430.227s 音频，无 DTS 警告；音频频率/能量与视频逐帧 hash 回归通过，硬件异构合成 6 项测试通过。音频有一次额外编码，成本与保真仍纳入整日验收，不能宣传为全轨无损。
4. **完整已有批次合成验证**：`frame_clock_v1/evidence/join_repaired.mp4`，5,295,593,807 字节；142 批拼接与全接缝验证通过，耗时 90.203s。这是原批次的恢复产物，仍包含旧批次累计 1.1s 的计划偏差，不作为最新逐样本修复的最终验收成片。
5. 原有三项滤镜字符串测试用 `trim=duration` 子串检查，实际上可以误匹配 `atrim=duration`。已替换为明确的视频 `trim=end_frame` 检查，并由实际帧/样本/PTS 测试承担行为证明。

## 当前执行句柄

启动前核对试验 src 与生产 src，仅 `pipeline.py` 的 ready queue 优先级不同；完整代码散列见试验 evidence/trial.json。当前主进程 PID 1824。待该进程结束后检查 elapsed、perf、数据库完成状态、成片帧/样本时间轴与识别差异，再运行最终全套测试和全片解码，避免额外负载污染主要计时。

完整正确性修复后的长文件优先冷运行已启动：`scratch/run_audit_20260320/analysis_lpt_trial`，工具 session **74323**；其 `evidence/process.json` 提供实际 PID，`elapsed.json` 仅在运行结束后生成。使用全新分析 DB、未复用批次。生产队列策略仍未改变。恢复任务 session 21090 已退出，旧拼接验证 session 64222 已结束；不得重启这些已完成任务冒充新基准。

## 测试集评估与调整

辅助代理仅做机械清点，主代理复核结论。初始 18 个测试文件、204 个函数、261 个参数化用例，无精确重复测试体。保留 NV/QSV、正常/异常以及集成/纯函数互补测试，不按名称相似删除。

移除 acceptance 中无结果断言的 cleanup 调用，将其替换为 shutdown 子系统中的 CUDA 清理失败后仍 checkpoint DB 与终止 FFmpeg 的行为断言。三个仅验证 UI 不崩溃的用例补充内容断言；表格测试固定终端宽度，避免截断造成假阴性。帧时长测试中旧的字符串 trim=duration 断言改为整数帧边界，并补充实际媒体解码验证。

另移除 testsample 的“至少 10 个文件且文件名合法”伪 E2E 检查：它没有解码/流水线行为且重复 scanner 解析覆盖。保留的 perf schema 测试迁入 `test_performance_regression.py`，使用独立 PerfCollector 而非重置全局对象；空壳 `test_acceptance_e2e.py` 删除。当前 17 个测试文件；默认剩余硬件 skip 为 6 项。
