# 2026-09-04

## 全面生产交付审查（未执行修改，仅审计）

- 审查范围：src 全部 16 模块、docs 10 篇、tests、config、pyproject、git 状态。
- 测试基线：`uv run pytest tests/` → 83 passed / 3 failed / 1 skipped（另有 test_scan_multiple_directories 偶发失败）。
- 关键发现：
  1. **P0 时序保序缺失**：docs/optimizations.md 与 PERFORMANCE_BENCHMARK.md Round 10 声称已实现 In-Order Sliding Window，但 `pipeline.py::_render_manager` 实际按完成顺序攒批，最终成片可能时间乱序；`test_in_order_sliding_window_dispatch` 为空洞测试（只断言批次索引，不断言文件归属）。
  2. **P0 detector.py 内嵌过时滤镜副本**：MotionDetector 使用 detector.py 内嵌的 EmaBackgroundModel/SpatialGridMotionFilter/AudioEnergyVAD（无 night mode、无全局闪光抑制）；filters.py 中才是 Round 10/11 新版 → 双源真相，生产走旧版，3 个测试因此失败。
  3. **P0 Round 9-14 全部变更未提交**：HEAD=f406fd6(Round 8)，scheduler.py/filters.py/ui.py/新测试套件均为 untracked，24 个旧测试已删未提交。
  4. **P1 tqdm→rich 未接线**：ui.py 已实现与用户 mockup 完全一致的 PipelineDashboard；pipeline.py 仍用 tqdm；process_date_cam 仍打 ASCII banner；`scripts/_patch_ui.py` 补丁脚本模式失配（不可直接运行）。
  5. **P1 信号量 acquire 无 timeout**（违反 AGENTS.md）：prescreen.py L74/L350、detector.py L776、ffmpeg.py L35/L75、renderer.py L389/L472。
  6. P2：批次断点复用测试失败（功能缺失）；docs/AGENTS.md 为陈旧副本；TESTING.md 宣称 84 用例全绿与实际不符；yolo_verifier 生产代码内嵌 Mock 兼容分支；renderer 超时路径遗留临时文件。
- 用户工作流：先审方案，确认后以「修复/实施」关键词触发执行。

## 修复执行（software-homevlog-hardening 团队）

- 用户批准修复后按 SOP 建团队。寇豆码（engineer 子代理）完成阶段1+2：基线 commit `691cfec`（Round 9-14 全量固化）+ 修复 commit `5c4df79`（filters 去重、acquire_with_retry 超时化、保序滑动窗口、批次断点复用、空洞测试重写、yolo Mock 分支删除），86 passed / 0 failed。
- QA 子代理两次派发失败（agent 类型不可用 + 子代理配额 429 限流，约 24h 后重置），主理人直接接管 QA 与阶段3。
- 阶段3（commit `cffcfb8`）：pipeline.py tqdm→PipelineDashboard 全量接线（三 worker 更新点 + set_queue_status + set_scheduler_state + register_dashboard 告警桥 + 断点种子回填）；process_date_cam 换 print_startup_banner/print_summary_card/print_error_summary；run_pipeline 加批量 banner；pyproject 移除 tqdm（uv.lock 零引用）；删除 scripts/_patch_ui.py 与 docs/AGENTS.md 陈旧副本；TESTING/architecture/PROJECT/hardware_scheduling_plan 四篇文档与实现对齐。
- 最终回归：compileall OK，86 passed / 1 skipped；benchmark --operator 8.43μs 无恶化；main.py --scan 与编排器空跑冒烟均通过。
- 遗留事项：AlternativePlan.md 被基线 commit 带入（项目背景参考文档，无害保留）；PRD/Requirements/ORIGINAL_REQUEST 三篇需求文档重叠待收敛；test_scan_multiple_directories 偶发失败未再现（pytest atexit PermissionError 是 Windows 临时目录清理噪音，非测试失败）。
