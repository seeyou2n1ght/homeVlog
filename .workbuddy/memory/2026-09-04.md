# 2026-09-04

## 全面生产交付审查（未执行修改，仅审计）

- 审查范围：src 全部 16 模块、docs 10 篇、tests、config、pyproject、git 状态。
- 测试基线：`uv run pytest tests/` → 83 passed / 3 failed / 1 skipped（另有 test_scan_multiple_directories 偶发失败）。
- 关键发现：
  1. **P0 时序保序缺失**：docs/optimizations.md 与 PERFORMANCE_BENCHMARK.md Round 10 声称已实现 In-Order Sliding Window，但 `pipeline.py::_render_manager` 实际按完成顺序攒批，最终成片可能时间乱序；`test_in_order_sliding_window_dispatch` 为空洞测试（只断言批次索引，不断言文件归属）。
  2. **P0 detector.py 内嵌过时滤镜副本**：MotionDetector 使用 detector.py 内嵌的 EmaBackgroundModel/SpatialGridMotionFilter/AudioEnergyVAD（无 night mode、无全局闪光抑制）；filters.py 中才是 Round 10/11 新版 → 双源真相，生产走旧版，3 个测试因此失败。
  3. **P0 Round 9-14 全部变更未提交**：HEAD=f406fd6(Round 8)，scheduler.py/filters.py/ui.py/新测试套件均为 untracked，24 个旧测试已删未提交。
  4. **P1 tqdm→rich 未接线**：ui.py 已实现与用户 mockup 完全一致的 PipelineDashboard；pipeline.py 仍用 tqdm；process_date_cam 仍打 ASCII banner；`scripts/_patch_ui.py` 补丁脚本模式失配（不可直接运行）。
  5. **P1 信号量 acquire 无 timeout**（违反 AGENTS.md）：prescreen.py L74/L350、detector.py L776、ffmpeg.py L35/L75、renderer.py L389/L472。
  6. P2：批次断点复用测试失败（功能缺失）；docs/AGENTS.md 为陈旧副本；TESTING.md 宣称 84 用例全绿与实际不符；yolo_verifier 生产代码内嵌 Mock 兼容分支；renderer 超时路径遗留临时文件。
- 用户工作流：先审方案，确认后以「修复/实施」关键词触发执行。

## 修复执行（software-homevlog-hardening 团队）

- 用户批准修复后按 SOP 建团队。寇豆码（engineer 子代理）完成阶段1+2：基线 commit `691cfec`（Round 9-14 全量固化）+ 修复 commit `5c4df79`（filters 去重、acquire_with_retry 超时化、保序滑动窗口、批次断点复用、空洞测试重写、yolo Mock 分支删除），86 passed / 0 failed。
- QA 子代理两次派发失败（agent 类型不可用 + 子代理配额 429 限流，约 24h 后重置），主理人直接接管 QA 与阶段3。
- 阶段3（commit `cffcfb8`）：pipeline.py tqdm→PipelineDashboard 全量接线（三 worker 更新点 + set_queue_status + set_scheduler_state + register_dashboard 告警桥 + 断点种子回填）；process_date_cam 换 print_startup_banner/print_summary_card/print_error_summary；run_pipeline 加批量 banner；pyproject 移除 tqdm（uv.lock 零引用）；删除 scripts/_patch_ui.py 与 docs/AGENTS.md 陈旧副本；TESTING/architecture/PROJECT/hardware_scheduling_plan 四篇文档与实现对齐。
- 最终回归：compileall OK，86 passed / 1 skipped；benchmark --operator 8.43μs 无恶化；main.py --scan 与编排器空跑冒烟均通过。
- 遗留事项：AlternativePlan.md 被基线 commit 带入（项目背景参考文档，无害保留）；PRD/Requirements/ORIGINAL_REQUEST 三篇需求文档重叠待收敛；test_scan_multiple_directories 偶发失败未再现（pytest atexit PermissionError 是 Windows 临时目录清理噪音，非测试失败）。

## 仪表盘 UI 打磨（commit `21a1ca3`）

- 用户反馈截图：开局出现幻影"已成片 17"（比率推算 `render_done/render_total × total` 所致）、刷新洪泛、徽标折行。
- 修复：堆叠条改为文件级真实计数（render_batch_dispatched/finished 记账 + 粗筛在飞 = prescreen_total - done - queue_qsize）；update_view 0.12s 节流 + Live auto_refresh 5fps + 0.5s 脉冲线程；徽标缩短（🎬 RENDER PREEMPT）；渲染行显示"上批 Xs │ 均速"；Batch on NVENC 命名；删除未使用的 Progress 实例；ultralytics logger 降为 WARNING 抑制摘要噪音；表头列宽 2:2:3。
- 验证：两个场景渲染核对（开局 0 成片、复刻截图时刻 8/8/9/61 守恒），86 passed。
# 2026-09-04

## 全项目多维度深度审计（评审团队模式）
- 审计范围: main.py + src/ 全 16 模块 + config + tests，基线: compileall 通过，pytest 86 passed/1 skipped (8.88s)。
- 确认 P0 缺陷 4 项: ①YOLO 验证帧索引 fps 不匹配 (pipeline.py:224 传 detector.fps=5，实际 adaptive effective_fps 0.5~5，>120s 文件验证失效); ②prescreen worker 异常分支未回传 render 消息→保序滑窗死锁 (pipeline.py:166-180); ③分片分析时全量音频重复 VAD 且 chunk_offset 时间戳错位 (detector.py:577-584); ④主线程 join(timeout=3600) < 渲染超时 7200s，超时静默丢批次。
- 配置漂移: detection.analysis_fps_tiers.ultra_long=0.5 配置存在但 detector.py:407-412 仅 short/medium/long 三分支，ultra_long 从未生效。
- 性能实证: 审计期间观察到活动渲染批次 (temp/_batch0_20260902_cam0) 实测 speed≈0.48x，证实静态段"hybrid_keyframe"名不副实——静态段仍全帧硬解+hwdownload，渲染是当前最大瓶颈。
- 死代码清单: renderer.render_vlog 链路 (~250行)、prescreen.run_prescreen_for_cam (~100行)、db 三个仅测试引用方法、_render_worker qsv 分支、配置项 render_retry_max/qc_validate/prescreen_auto_calibrate/dynamic_batch_sizing 未接线。

## 阶段Ⅰ正确性加固（已实施并回归通过）
- P0-1: pipeline.py yolo verify 改用 last_perf.effective_fps；detector.py yolo_sample_interval 移至 effective_fps 确定后并以其为基准。
- P0-2: pipeline.py _prescreen_worker 异常分支补发 render FAILED 消息，消除保序滑窗死锁。
- P0-3: detector.py 分片分析按 chunk 时间窗切片音频，消除 VAD 时间戳错位与 O(N^2) 重复检测。
- P0-4: pipeline.py 线程 join 去掉 3600s 超时（渲染看门狗兜底），消除静默丢批次。
- P1-6: detector.py 补 ultra_long 档分支（>long_max 时生效）。
- P1-7(分析侧): analyze_frames 末尾追加 STATIC 闭合标签，末帧时间戳 == start_offset+file_duration。
- 新增 tests/test_phase1_regression.py 5 用例；全量 91 passed / 1 skipped (4.96s)。
- 待办: 阶段Ⅱ 渲染静态段关键帧抽取（实测瓶颈 0.48x）；阶段Ⅲ 死代码/配置清理；阶段Ⅳ YOLO 推理锁、字幕 ramping 精确映射、README 校准。

## 生产运行诊断（只读，20260902 全天运行 01:42-02:11）

- 用户报"卡住 GPU 高位"：**实际未卡住**——末批次 batch10（含 3237s 怪物文件片段）NVENC 渲染期间无日志输出属正常（perf 仅结束时写）。运行于 02:11:42 正常完成，全程 28m51s，SLA 内。
- **重大发现（pre-existing bug，Round 6 heavy/light 路由引入，与本次及并行会话修复均无关）**：`_render_worker` 哨兵中毒——all_dispatched 后向 heavy/light 队列各投 2 个 None 哨兵；NV worker `heavy.get_nowait()` 拿到 None 即 `break`（pipeline.py 约 L403），**不检查 light 队列剩余真实批次**。本案例：batch3/batch4（全 STATIC 轻批次，文件 24-39，09:53~12:11 约 2.3h 白天内容）滞留 light 队列被静默丢弃——无 ffmpeg 错误、无 warning、无 perf 记录（9/11 批），成片缺该时段且 DB 标记 COMPLETED。证据链：perf_20260902_cam0_20260904_021142.json 缺 batch3/4 + temp/ 无对应文件 + error log 0 字节 + DB 文件构成验证（batch3/4 全 STATIC=light，后续批含 DYNAMIC=heavy 被优先抽干）。
- **历史输出可能同样缺段**（该 bug 自 Round 6 起存在，是否触发取决于收尾时 light 队列是否有剩余）。
- 修复方案（待触发）：item is None 改为仅当双队列均空才 break 否则 continue；渲染收尾加 dispatched vs rendered 对账告警；补回归测试。
- 注意：另一并行会话同日在做"评审团队模式"审计与阶段Ⅰ加固（未提交，改了 pipeline/detector 等），修复时需基于最新工作树。
- 其他：DB updated_at 混用 UTC（datetime('now')）与 localtime（schema 默认），读数注意 +8h；输出文件被用户改名 -old 后缀。

## 阶段Ⅱ/Ⅲ/Ⅳ 全部完成（99 passed / 1 skipped, 4.5s）
- 阶段Ⅲ 瘦身: renderer 543→231 行（删 render_vlog 等 6 个死函数）、prescreen 590→482（删 run_prescreen_for_cam）、database 283→226（删 4 个死方法+时区统一 localtime）；pipeline 删 qsv 渲染死分支/未用导入；settings.yaml 删 5 个未接线键；ffmpeg.run_ffmpeg 修 UnboundLocalError；utils 新增 cleanup_temp_artifacts 并在 run_pipeline/--date 启动时调用。
- 阶段Ⅱ 渲染快路径: timeline.build_concat_filter 对纯静态且段长>=2*kf 的文件启用 select 抽帧置于 scale/hwdownload 之前（剥离文件链尾部 fps）；冒烟实测 90s 静态→1.5s 成片，0.8s 渲染完成，ffmpeg+NVENC 真实链路接受。
- 阶段Ⅳ: yolo_verifier 加类级 _inference_lock；timeline 新增 compute_display_plans/src_offset_at_display 单一来源，build_concat_filter 与字幕共用；generate_timecode_subtitles 改用展示计划+ramping 逆映射；SRT 字幕接入 process_date_cam 成片后生成；README 指标校准。
- 新增测试: 快路径滤镜图 2 例、展示计划/ramping 逆映射 5 例；benchmark --operator 通过 (7.07x/99.8%)。
- 注意: 渲染快路径改动后首跑需观察真实 NAS 素材的批次耗时对比（旧基线 0.48x）。

## UI 术语业务化改造（commit `1b35747`，本团队会话）

- 用户批准实施术语优化：仪表盘徽标中文化（🎬 渲染优先/⚡ 双卡协同/🍃 常规核显解码）、待办队列（粗筛/分析/渲染）、图例六桶（已完成/渲染中/AI 分析中/待渲染/粗筛中/待处理）、阶段命名（①快速粗筛 ②AI 精析 ③视频渲染）、"闭环"→"已完成"；速率文案统一（帧/秒（核显解码）、批次 N（NVENC 编码））；Banner/总结卡去秒数冗余、硬件分工中文化。
- main.py：删 (SUCCESS)/(FAILED) 后缀；`logging.shutdown()` 移到最终打印之前，作为 [0m ANSI 泄漏的防御性修复（本地未能复现，待下次真机运行验证）。
- 注意：该 commit 的 pipeline.py 同时携带了并行会话阶段Ⅰ加固改动（commit message 已注明）；并行会话阶段Ⅱ/Ⅲ/Ⅳ 的其余文件（detector.py/renderer.py/timeline.py 等）在我提交时仍未提交，后续由谁提交需用户知悉。
- 回归：100 passed / 1 skipped。
- 悬而未决（用户说"先放一下"）：_render_worker 哨兵中毒丢批 bug 修复方案已备好待触发；重跑 20260902 补齐 09:53~12:11 缺失时段。

## 静默丢批修复（并行会话诊断确认的存量 bug，Round 6 引入）
- 根因: _render_worker 拿到 heavy 队列 None 哨兵即 break，滞留 light 队列的纯静态批次被 stranded 且零上报（生产实例: 20260902 batch 3/4 丢失 09:53~12:11 约 2.3h 内容）。
- 修复①: 哨兵降级为纯唤醒信号——仅当 all_dispatched && heavy.empty() && light.empty() 才允许退出，否则 continue。
- 修复②: 渲染收尾对账——dispatched_batch_ids vs (produced | terminal)，缺失即 _add_error("render batches silently dropped: [...]")；失败/空时间轴批次计入 terminal_batch_ids 去重。
- 回归测试: test_light_batches_not_stranded_by_sentinels（8 STATIC 文件→4 轻批次+慢渲染 mock）。双向验证: 旧代码下失败且对账准确报 missing=[2,3]，修复后 3 连跑稳定通过。全量 100 passed / 1 skipped。
- 注意: 修复期间 pipeline.py 曾被外部回退（mtime 02:36，哨兵代码被改回旧版，对账代码保留），已重新应用。多会话并行编辑同一文件需警惕。
- 待用户决策: 重跑 20260902 补齐缺失时段（旧产物已改名 DailyVlog_20260902_cam0-old.mp4）。

## 项目清理、文档对齐与提交（commit 16dcba1）
- 清理: AlternativePlan.md → docs/ 归档；conftest 移除僵尸配置键；确认 .gitignore 已覆盖 temp/output/logs/data。
- 文档对齐: README/TESTING/PROJECT/PERFORMANCE_BENCHMARK 全部校准为 9 模块 100 passed + 1 skipped；TESTING.md 补充 phase1_regression 模块与快路径/对账覆盖说明。
- 新增 .workbuddy/memory/MEMORY.md 沉淀长期工程约定（哨兵语义、对账机制、快路径、配置纪律、文档同步）。
- 提交: 23 files, +653/-567；提交前 compileall + pytest 全绿。

## 7 天生产运行（20260320-20260326）数据分析结论
- 正面: 渲染对账零静默丢批；快路径在多数批次生效；7 天 COMPLETED；单日 29.6~50.4 min。
- 严重问题①: 137 个文件预筛 FAILED——QSV 信号量饥饿（8 分析 worker 长占用 vs 90s 预算），文件被永久丢弃且 FAILED 不可重试（COMPLETED 天整体跳过）。根因: _prescreen_keyframes 实为 CPU 解码却占 QSV 槽。
- 严重问题②: 分析阶段均值 243.5s/文件，其中解码 243.5s vs CPU 分析仅 6.0s——PyAV 全帧解码+全量 4K 下载是主瓶颈（采 660 帧解码 9900 帧）。
- 严重问题③: 字幕接线 NameError（all_rows→all_tasks，已修 dac8944）；0323 batch0 OOM(-12)、0324 batch1 NV 信号量 abort、0326 batch8 signal 15，失败日仍 COMPLETED；0327 半成品。
- RAM 57-64% (18-20GB)，解码内存池+4K filtergraph 是 OOM 温床。
- 下一阶段方向(待确认): 预筛改专用 IO 信号量；FAILED 自动重试/恢复；分析解码改 ffmpeg 管道 (fps+scale_qsv 下采样后再下载)；渲染失败日状态降级可重跑。

## 按优先级修复落地（commit 915567a，102 passed）
- #3 解码管道化 A/B 实证（NAS 317s 文件）: PyAV 92.6s → pipe 14.5s (6.4x)，帧数 633/634、yolo_buf 159、音频采样完全一致；benchmark --operator 通过。
- #1 预筛 disk sem 解耦 + 30s×6；#2 reset_failed_tasks (retry<=3) + 渲染失败日降级 FAILED；#4 租约后渲染激活则 cuda→qsv 降级 + 渲染 NV 预算 30s×10；字幕配置关闭。
- 已知 flake: test_scan_multiple_directories 偶发 Windows pytest-temp PermissionError（测试基建问题，非业务）。
- 下一步: 重跑 0320-0327 验证单日 <25min 目标并补齐 137 丢失文件；渲染阶段 (~26min/天) 成为新长杆，如需再压需评估批次构成/编码参数。
