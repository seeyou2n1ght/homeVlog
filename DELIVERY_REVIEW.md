# HomeVlog 交付审查

审查日期：2026-09-20。审查对象为当前工作区，包含用户已有的 5 个未提交文件。未修改生产代码、生产配置、数据库和成片。

**结论：不建议将当前版本标记为交付验收完成。** 主流水线已经具备资源租约、批次原子替换、失败对账和人工反馈持久化等基础，但输出校验、像素语义、媒体元数据和审核重渲染仍有可复现的正确性缺口。优先修复这些契约，再接受性能收益。

> 修复状态（2026-09-21）：本报告第 1–12 项的正确性、安全边界、配置契约和可追溯性修复已落地；原缺陷复现已反转并迁入现有测试。软件 FFmpeg 已验证动态提前 EOF 的音视频同钟。全库 F401/F841 历史债务和流水线大模块拆分未在本轮扩散处理。本报告的整日逐帧解码、目标播放器、真实硬件同条件 A/B 和人物识别 holdout 仍未重跑，因此总体交付结论保持不变，直至这些真实环境门禁通过。

P1 表示应在交付前修复；P2 表示应列入明确的后续收敛计划。本轮没有足够证据认定已经发生不可恢复的生产数据丢失，不列 P0。

## 验证范围与证据

- 实际执行：常规测试 **245 passed、7 skipped，21.84s**；配置范围内 Ruff 通过；编译检查通过；`git diff --check` 通过。
- 新增隔离审查复现共 **11 项**，分三次执行全部通过。这些断言用于**证明缺陷仍然存在**，不是修复验收；修复后需反转相应断言，迁入现有子系统测试。
- 复现脚本：`temp/delivery_audit/test_delivery_audit.py`。其中媒体测试使用真实 PyAV/软件 FFmpeg；其余包括临时数据库、受控队列/故障注入和只读生产快照。
- 未重新执行整日流水线、GPU 冒烟、整片逐帧解码、目标播放器检查或人工 holdout。本轮通过的常规测试不能证明这些项目通过。
- 审阅入口涵盖 scanner → prescreen → detector/YOLO → timeline → scheduler/render → finalization，以及 DB、进程生命周期、审核重渲染、监控、配置和 CI。

当前第一手产物：

| 项目 | 当前读取结果 |
|---|---|
| `20260320` 数据库任务 | 142 个，名义源时长 89,333 秒；44 STATIC、98 SUSPICIOUS/ANALYZED |
| 当日数据库成片状态 | COMPLETED |
| MP4 | 4,710,354,488 字节，1920×1080，HEVC + AAC |
| 视频轨 / 音频轨时长 | 14,099.479980 / 14,099.501333 秒，末端差约 21.35 ms |
| 现存 perf 文件 | `logs/perf/perf_20260320_cam0_20260920_141752.json` |
| perf 中的流水线时长 | 1,795.84 秒；该文件没有 `final_concat` 阶段记录，不能独立证明最终交付全流程耗时 |
| QSV 统计 | 88 批次，空闲 142.28 秒，worker utilization 91.9%，拒收 62 次 |
| NV worker utilization | 95.5%、96.2% |

音视频轨末端接近，只说明末端时长接近，不证明整片无断帧、无局部音画偏移或人物事件完整。

## 1. P1：输出有效性门槛允许明显短片及缺音轨通过

位置：`src/hardware/render_cache.py:67-105`；调用方 `src/stages/renderer.py:862` 附近及 `src/pipeline.py:1520` 附近。

`valid_video()` 只要求能解出第一帧，并采用 `max(5s, expected_duration × 11%)` 的时长容差；不要求音轨存在、不校验音视频各自结束时间。普通批次校验也不解码尾部。约 14,100 秒的预期成片，可容忍约 1,551 秒偏差；短批次则受 5 秒下限影响更大。

**真实媒体复现**：生成 8 秒、无音轨的 MP4，传入 `expected_duration=12`，返回 True。不是 Mock。

修复：以共享 display plan 为基准，分别校验视频、音频、帧率/分辨率和尾部；容差按帧时间基及已声明的源缺失策略定义，不能按成片百分比放宽。音轨即使是合成静音也须满足交付音频契约。区分“媒体可打开”和“渲染产物满足计划”。

验收：短片、无音轨、尾部不可解码、音视频末端不齐分别拒绝；正常短批次不因体积小误拒。按整数帧数定义输出计划，避免要求无法在输出帧率表示的毫秒精度。

## 2. P1：接缝校验仅检查窗口两端，漏掉内部断帧

位置：`src/hardware/render_cache.py:43-60, 112-126`。

校验只检查 `seen[0]` 和 `seen[-1]` 是否覆盖接缝前后 0.5 秒，未检查中间相邻 PTS。窗口两端各有可解码帧，不代表窗口连续。

**真实媒体复现**：生成 20fps 视频，删除接缝附近 15 帧但保留两端，最大 PTS 间隔为 **0.8 秒**；`valid_video(..., checkpoints=[2])` 仍返回 True。

修复：检查相邻时间戳单调性、最大间隔及预期帧覆盖。顺序和并行路径共用同一段校验逻辑，避免目前复制代码继续漂移。

验收：接缝中部缺帧、重复/倒退 PTS、损坏帧均失败；并行与串行得到相同结果。接缝抽查不替代交付前的整片解码验收。

## 3. P1：Y 平面快路径改变灰度语义，并错误处理 10 位像素

位置：`src/stages/prescreen.py:151-157`；`src/stages/detector.py:764-782`。

判断条件只有第一平面和 stride 存在，没有限定像素格式、位深、亮度范围。YUV 的第一平面不等于所有输入的 8 位灰度。对于有限范围 YUV，直接读取 Y 还会改变旧灰度转换的数值范围，而运动阈值仍沿用原值。

**PyAV 实际帧复现**：

| 输入 | 新快路径 | 原 `to_ndarray(format="gray")` |
|---|---:|---:|
| Y=16 | 16 | 0 |
| Y=32 | 32 | 19 |
| Y=235 | 235 | 255 |
| 10 位 Y=512 | 被按字节解释为交替 0、2 | 应走对应位深转换 |

8 位路径已证明数值语义改变，10 位路径已证明解析错误；**本轮未据此宣称已测出实际人物漏检率**。

修复：共享一个受像素格式/位深/范围约束的灰度转换入口；不满足约束则走现有转换。要保留直接 Y 读取，必须固定算法输入契约并重新校准相关门限。先保证语义一致，再保留测得的优化。

验收：8 位 limited/full range、10 位及 RGB 输入对照；另用独立标注集复核日间动作、夜间微动和边角小目标召回。

## 4. P1：文件名时长被当成已探测媒体时长，动态 EOF 修复未闭环

位置：`src/stages/scanner.py:158-165` → `src/pipeline.py:345-349, 541-551` → `src/stages/detector.py:849-868`；`src/stages/timeline.py:1059-1078`。

scanner 将文件名起止差写入 `file_duration`。预筛随后补上 `has_audio`。detector 只在时长非正或音频信息未知时探测媒体，于是普通首次运行就可能跳过真实时长探测，并把文件名时长记录为 `file_duration_detected`。

最新未提交修复只给变速段补 `tpad`，`DYNAMIC/DYNAMIC_AUDIO` 仍仅 trim。它没有覆盖同样会提前 EOF 的常速段。

**受控复现**：提供正时长和已知音轨标记，detector 不调用媒体探测，直接接受该时长。**真实 FFmpeg 复现**：8 秒源视频按 12 秒 DYNAMIC 计划渲染，得到视频 8 秒、音频 12 秒，且当前校验通过。

修复：区分名义切片区间和已探测媒体时长，复用预筛/分析已打开的容器持久化媒体数据，不给 scanner 增加逐文件探测。显式记录缺失区间；由 timeline 决定缩短或补帧策略，所有状态共享同一输出时钟。补帧只能填补展示，不能伪装成原始内容完整。

验收：文件名跨度大于/小于实际媒体、末段 DYNAMIC、纯静态、变速段、缺音频、整个区间无帧分别验证；字幕和元数据必须使用相同策略。

## 5. P1：QSV 放宽窃取与执行前拦截互相矛盾

位置：`src/pipeline.py:940-949, 1068-1080`。

尾部允许以 1.5×门限从队列取任务，但构建时间轴后仍按原 420 秒门限把任务放回同一个队列。500 秒任务可被反复取出、查询整日数据库、重建时间轴、再放回；NV 尚未空闲时不会产生渲染进展。放宽门限没有真正作用于编码阶段。

**受控队列复现**：500 秒任务按 630 秒门限取出，按 420 秒执行守卫退回，连续三轮仍留在队列。尚未将其冒充为真实 GPU 活锁实测。

修复：准入和执行共享同一资格判定。最低风险方案是先取消无效的放宽；如保留放宽，将实际批准上限随任务传递。确认不可由 QSV 处理的任务不得重新进入 QSV 可窃取集合。

验收：420/421/500/630/631 秒边界；NV 忙/闲、队列尾部、真实分类与估计不一致；同批次不得无进展反复退回。收益另做同状态 A/B。

## 6. P1：审核重渲染绕开主流水线的完整性和时间轴契约

位置：`scripts/audit_tool/rerender.py:44-47, 163-180, 278-304`；`src/stages/timeline.py:773-776`；`src/stages/companion.py:89-92`。

主流水线会阻止上游缺失/失败时发布整日成片；审核入口仅在全部文件缺失时阻止，部分缺失则自动过滤，最终仍标记 COMPLETED。伴随资产却重新读取全部数据库行，未使用实际过滤后的素材/时间轴，可能给出无法对应成片的位置。

审核入口还调用默认跨文件重分类并写回 DB 的 `build_timeline()`，主流程和伴随资产则采用逐文件 `resolve_presence=False`；因此“审核后重渲染”不是同一执行契约的另一个入口。

**受控复现**：两个文件缺一个，可访问性检查仍允许继续。其余完整调用链已静态核对，未重新压制生产素材。

修复：复用主流程的完整性校验和只读逐文件 timeline 构建。默认阻止缺素材；确需部分导出时采用明确的 PARTIAL 状态和缺失清单，伴随资产直接消费实际渲染快照。不要为审核入口维护另一套完成语义。

验收：部分缺失、全部缺失、审核覆盖、跨文件 presence、重复重渲；成片、字幕、元数据覆盖文件集合与时钟必须一致，构建时间轴不得意外写回分析事实。

## 7. P1：取消单个审核任务会污染整个进程

位置：`scripts/audit_tool/rerender.py:77-86`；`src/hardware/ffmpeg.py:45-54`。

`cancel_task()` 调用全局 `kill_all()`，不仅杀掉同进程其他渲染任务，还设置全局 `_interrupted=True`。审核任务启动路径没有恢复该状态；后续批次会在渲染入口直接返回。

**隔离进程复现**：取消一个任务后，全局 interrupted 为 True。测试没有运行或终止生产进程。

修复：单任务取消只操作该任务所属进程/取消令牌；全局中断保留给应用退出。短期若暂不实现任务级进程分组，应显式限制审核服务只运行一个任务，并在所有旧任务确认退出后才恢复全局状态。

验收：取消 A 时 B 不受影响；A 取消后 C 能正常启动；不能用无条件 reset 清掉仍在执行任务的终止信号。

## 8. P1：伴随资产失败仍标记完成，恢复快路径不会补齐

位置：`src/stages/companion.py:107-108, 179-182`；`src/pipeline.py:1519-1524, 1614-1629`。

元数据/字幕写入异常被吞掉；主流程先存视频 manifest，再生成伴随资产，随后无条件标记 COMPLETED。下次运行只核对视频 manifest，可直接跳过。直接 `write_text` 还可能留下半份 JSON，关闭字幕后旧 SRT 也未核对版本。

**故障注入复现**：数据库读取失败时资产函数正常返回 None，元数据不存在；调用方不检查结果。

修复：资产文件临时写入后原子替换；返回结构化结果或抛出失败；把必需资产纳入完成条件和恢复校验。媒体已成功但资产失败应可只补资产，避免整日重渲。关闭字幕时应处理不匹配的历史字幕。

验收：磁盘满、写入失败、元数据截断、字幕缺失、重启恢复；不得出现“成功且永久缺资产”。

## 9. P2：空分析结果导致双份分段数据不一致

位置：`src/core/database.py:289-306`；`src/stages/timeline.py:548-575`。

`set_analysis_result([])` 更新 JSON，却仅在新列表非空时删除旧 segments。timeline 优先读关系表，因此继续使用旧分类。

**真实临时 SQLite 复现**：先写 60 秒 DYNAMIC，再写空列表，得到 `analysis_segments='[]'`，但 segments 仍有一行。

修复：先定义空成功结果是否允许。若允许，替换事务必须清除旧算法 segments；若不允许，应拒绝提交。非法 JSON 不能被静默解释为空列表。保留独立 human_reviews。

验收：非空→空、非法 JSON、事务回滚及人工反馈保留。中期减少 JSON/关系表双权威，不要求此次大改数据模型。

## 10. P2：性能结论与当前可追溯证据不匹配，worker 指标混入等待和重试

位置：`docs/PROGRESS.md:8` 起；`docs/BENCHMARK.md:66` 起；`src/pipeline.py:1088-1089, 1137-1158, 1179-1191`；`src/stages/companion.py:20-56`。

文档记载 1,452.84 秒、QSV 空闲 0.56 秒、利用率 100%；当前 perf 为 1,795.84 秒、142.28 秒、91.9%。它们可以是不同运行，但当前资料不足以复核文档中那轮改善。现存 perf 又缺 final_concat，不能把它与稍后成片直接拼成一个端到端 A/B。

另外，worker busy_time 从批次开始计时，包含查询、构图、staging、租约等待和验证，不是硬件编码引擎利用率。QSV worker 内直接执行 NV 重试，却继续记入 QSV worker busy/batch 统计；该成功重试分支未补对应的 `render` PerfRecord，也未同步 NV 活跃计数。这会污染下一轮性能归因。

修复：每次运行固定 run_id、代码/配置/分析快照指纹和产物 identity；区分成功、失败、仅重渲、仅合成。分别记录 worker 占用、排队、租约、编码、校验和重试。QSV 失败后交回 NV 队列更容易维持统一计数和调度语义。

性能释放顺序：先去掉第 5 项无效循环，再将每批全日查询缩为目标文件查询；最后利用同条件实测判断调度排序/并发收益。当前高 worker 占用不能支持继续增加 NVENC 路数，也不能把累计排队秒数当成可节约壁钟。

验收：同数据库、同 timeline、同编码、同硬件下对照，并同时检查事件覆盖、音频、体积、解码完整性和真实 wall-clock；保留原始数据。未完成 holdout 不得宣称识别准确度不变。

## 11. P2：本地审核 API 的信任边界未收敛

位置：`scripts/audit_tool/app.py:49, 72-76, 194-250`；`scripts/audit_tool/service.py:224-268`。

服务绑定 127.0.0.1 是正确的，但同时开放任意 Origin、POST 及 Content-Type，没有 token/Origin/Host 校验。接口可修改审核结果、发起 GPU 重渲和取消任务；媒体接口还接受调用方提供的本地路径。这里不能把“本机地址”当成调用授权。

这是静态边界审查发现，未实施浏览器跨站攻击；浏览器本地网络访问策略是否额外拦截取决于实际环境。

修复：默认同源、拒绝不受信任 Origin/Host，使用启动时随机 token 保护变更操作；媒体参数只接受已登记文件或允许目录内已 resolve 的路径，限制请求大小与数值参数。打开资源管理器使用参数数组而非 shell 字符串。

验收：拒绝外站 Origin、无 token 写请求、未登记路径和异常范围参数；正常本机审核流程不受影响。

## 12. P2：配置与代码规范需要以实际契约收敛

- `src/core/config.py:88-91` 只检查 QSV analysis ≤ total，未检查 analysis + render ≤ total；独立 semaphore 实际可超过声明总量。验证总量关系并要求并发值为整数。
- `src/pipeline.py:1209` 的异构模式最多创建 3 个 worker，而配置允许任意正的 `render.max_concurrency`。要么校验支持的范围，要么让该值具有明确语义，不能静默忽略。
- `pyproject.toml:41` 的 Ruff 仅启用 E9/F63/F7/F82。这次“Ruff 通过”意味着有限的致命错误检查通过，不意味着完整规范审查通过。建议先补 F401/F841 等有直接维护价值的规则，再按新增变更渐进执行，避免全库格式化淹没交付修复。
- `pipeline.py` 内 `_render_manager()` 同时处理队列准入、重分类、编码重试、计数和最终对账，重复入口已经产生第 5/6/10 项实际问题。优先抽取这些重复契约的普通函数；不引入通用工作流框架、不重写 scheduler。
- 接缝校验存在串行/并行两份算法；媒体灰度转换存在两份快路径。修复时共用小函数即可，透明 facade 暂无交付必要删除。

## 建议修复顺序

1. **输出正确性**：第 1、2、4 项一起修，严格计划与媒体验证先建立闭环。
2. **识别语义**：第 3 项修复格式/range 合同，完成标注集回归后才能认可零拷贝收益。
3. **统一执行和恢复**：第 5–8 项；审核入口复用相同 timeline、任务取消和提交标准。
4. **持久化及测量收敛**：第 9–12 项；在可信指标基础上做下一轮性能优化。

交付验收至少要求：上述真实媒体负例被拒绝、未提交的 EOF 修复覆盖所有状态、取消后可重试、资产故障可恢复、同一快照的整日成片完整解码与播放器检查、人工 holdout、可追溯性能对照。不要以本轮或历史绿色测试替代这些检查。
